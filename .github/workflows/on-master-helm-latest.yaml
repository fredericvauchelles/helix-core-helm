name: Release Charts

on:
  push:
    branches: [ "master" ]
    paths:
      - 'chart/**'
  pull_request:
    branches: [ "master" ]
    paths:
      - 'chart/**'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/workflows/helm-chart-build-push.yml